import { BankAccountDirective } from './bank-account.directive';

describe('BankAccountDirective', () => {
  it('should create an instance', () => {
    const directive = new BankAccountDirective();
    expect(directive).toBeTruthy();
  });
});
